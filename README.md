# Dotfiles

dotfiles repository managed with GNU Stow.

## Installation

To install the dotfiles, clone this repository and use GNU Stow to create symlinks in your home directory:

```bash
cd ~
git clone git@github.com:YohanDecamps/dotfiles.git
cd dotfiles
stow */
```

